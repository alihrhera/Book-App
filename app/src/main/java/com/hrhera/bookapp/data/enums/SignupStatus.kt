package com.hrhera.bookapp.data.enums

enum class SignupStatus {
    SUCCESS,
    NAME_ERROR,
    EMAIL_ERROR,
    PASSWORD_ERROR,
    PASSWORD_INVALID,
    PHONE_INVALID,
    SOME_ERROR,
    ALREADY_USER,
    LOADING
}