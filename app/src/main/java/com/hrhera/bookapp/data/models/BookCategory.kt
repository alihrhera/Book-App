package com.hrhera.bookapp.data.models

import java.util.*

data class BookCategory(var id: Int=0,
                        var name:String="",
                        var insertionTime: Date=Date(),
                        )
